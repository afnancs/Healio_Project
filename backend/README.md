# Healio local backend

## Run

```bash
cd backend
npm install
npm start
```

Then open:
- Frontend: http://localhost:4000/
- Backend status: http://localhost:4000/backend-status

Saved data lives in `backend/db.json`.
